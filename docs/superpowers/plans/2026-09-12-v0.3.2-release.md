# Needlbar v0.3.2 Release Plan

> **For agentic workers:** Use subagent-driven development for the bounded metadata task; root owns candidate integration and protected release operations.

**Goal:** Publish the user-verified Claude connection preflight as v0.3.2 and update Homebrew.

**Architecture:** Preserve the existing two-run release pipeline. Validate exact candidate G without a tag, then tag that same G and require independently validated publication. Download and verify public artifacts before updating distribution references.

**Tech Stack:** Swift, Rust, GitHub Actions, Developer ID/notarization, Homebrew Cask.

## Invariants

User authorized a new release on 2026-09-12 after verifying that valid existing
Claude login skips browser login. Use version 0.3.2 and build 5 in host/widget.
Preserve identifiers, App Group, signing, entitlements, secrets and environment
protections. Do not re-authenticate the user or modify Keychain ACLs. No force
push, retag, historical release edits, or native QA claims beyond the observed
existing-login success. Keep previous backups, logs, untracked work, and previews.

## Task 1: Release metadata and contracts

Ownership: metadata worker only changes Resources/Info.plist,
WidgetExtension/NeedlbarWidgetExtension-Info.plist, .github/workflows/release.yml,
scripts/tests/notarize-app-tests.sh, and new .github/release-notes/v0.3.2.md.
Root owns README, STATUS, plan, git operations. Existing auth changes are preserved.

- [x] Add current-release contract expectations for `0.3.2`, build `5`, and
  `.github/release-notes/v0.3.2.md`; run `bash scripts/tests/notarize-app-tests.sh`
  and record expected RED before metadata edits.
- [x] Change only both plist version/build values and workflow body_path.
  Write notes covering existing-login reuse, permission-only verification,
  auth-versus-network errors and upfront copy. Clearly retain first OS grant.
- [x] Preserve historical fixtures and prepared/public-state contract behavior;
  reject mismatched host/widget versions, builds and release-notes path.
- [x] Run focused shell contract and `git diff --check`, report RED/GREEN.

## Task 2: Candidate integration and validation

- [x] Independently review metadata and existing auth review evidence. Record
  user-confirmed existing-login behavior; no claim that all auth branches were
  manually verified.
- [x] Run fresh `PATH="/Users/taejunoh/.cargo/bin:$PATH" make test` on final
  candidate. Known Analytics admission timing flake is a gate failure if it
  occurs, not a passing result; investigate without weakening tests.
- [x] Commit exact auth and release changes, fast-forward local main preserving
  `.logs/` and other unrelated files. Check remote main, push without force.
- [x] Dispatch `gh workflow run release.yml --ref main`; record run/head SHA G.
  Stop for required user environment review, never self-approve or bypass it.

## Task 3: Publication and distribution

- [x] After tagless validation succeeds, verify remote tag absence and tag exact
  G with annotated `v0.3.2`. Push only that tag and require tag-run success.
- [x] Download public ZIP/sidecar, verify checksum, host/widget 0.3.2/5, arm64,
  matching Developer ID/runtime, strict signature, staple and Gatekeeper.
- [x] Update existing Homebrew tap Cask to 0.3.2 and measured public checksum;
  preserve arm64/macOS14/no-zap contract, verify it, and push it without force.
  Tap commit `6531e116833591d091b0bbda5409d899115b711b` is published; `brew style` and
  `brew audit --cask --online` both exited 0.
- [x] Update README public download and STATUS with actual run/hash evidence.
  Retain local installation record until public artifact is actually installed.

## Publication evidence — 2026-09-12

The exact candidate is `787865d1f22e1ec4a70daed5264f10740a38726a`.
Tagless validation run `34707625771` and tag-triggered publication run
`34708964704` both succeeded. Independent public verification recorded ZIP
SHA-256 `3db2e6bb7aadf7eb1bb26de29139b95073a866428d706f4fc08b6314e4b741c3`,
host/widget version/build 0.3.2/5, arm64 executables, macOS 14.0 minimum
target, Developer ID team `3BMF4LM6TM`, hardened runtime, strict signature,
stapled host ticket, Gatekeeper acceptance, and fixture/review-host exclusion.
The workflow logs contain a known Node 20 action-deprecation warning while
remaining successful. The canonical local installation remains the signed
v0.3.1/build 4 app at PID `54617`; the public v0.3.2 artifact was not installed.

## Conflict preflight

| Writer | Owned files | Resolution |
| --- | --- | --- |
| Metadata worker | Plists, workflow body_path, shell contract, new notes | No other writer |
| Root | README, STATUS, plan, git and release state | Separate files |
| Reviewer | Read-only | No mutations |
