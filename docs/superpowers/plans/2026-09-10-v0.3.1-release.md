# Needlbar v0.3.1 Release Plan

> **For agentic workers:** Use subagent-driven implementation for metadata and contract changes; root coordinates merge, protected release runs and installation.

**Goal:** Merge the approved Analytics updates and distribute v0.3.1, then reinstall the verified signed app.

**Architecture:** Preserve the existing two-run release workflow. Tagless validation of exact candidate G precedes an annotated tag on G; the tag run independently verifies and publishes its own artifact. Preserve all security gates and pending native-acceptance limitations.

**Tech Stack:** Swift/AppKit, Rust, Git submodules, GitHub Actions, Developer ID/notarization, Homebrew Cask.

## Authorization and invariants

User authorized merge, reinstall and release on 2026-09-10. Use patch version
0.3.1 and build 4 in both bundles. Do not change bundle IDs, signer, entitlements,
App Group, provider behavior, notarization credentials or environment protections.
Keep v0.3.0 notes, tag, assets and historical evidence unchanged. No force pushes.
Private brainstorming files stay local (excluded locally, not deleted).
Broader native UI and macOS14 widget/notification acceptance remain documented
limitations, not silently passed by signing or automated tests.

## Task 1: Release metadata and contracts

Files: Resources/Info.plist; WidgetExtension/NeedlbarWidgetExtension-Info.plist;
.github/workflows/release.yml; scripts/tests/notarize-app-tests.sh;
.github/release-notes/v0.3.1.md; README.md; docs/STATUS.md.

- [ ] First make the current-release contract require these exact values:
  host and widget CFBundleShortVersionString `0.3.1`, CFBundleVersion `4`,
  workflow `body_path: .github/release-notes/v0.3.1.md`.
- [ ] Run `bash scripts/tests/notarize-app-tests.sh` and record expected RED.
- [ ] Set both plist version/build pairs and the workflow body_path to the
  values above. Create v0.3.1 notes covering Analytics summary/comparison,
  expandable diagnostics, timestamp bounds and bridge decoding corrections.
- [ ] Preserve historical v0.3.0 contract fixtures and fixed notes. Add a
  current v0.3.1 contract with wrong-version/build/notes-path rejection cases.
- [ ] README must retain the existing public v0.3.0 download until v0.3.1 is
  actually published and verified; describe v0.3.1 as prepared meanwhile.
- [ ] Run the focused contract and `git diff --check`; independent review.

## Task 2: Reproducible merge and exact candidate

- [x] Audited and published pinned vendor commit `3c427e0558932e9c6d36948146059c9ca62cd525`
  on `taejunoh/tokscale-core` branch `codex/needlbar-v031-time-bounds`.
  Verified the remote SHA and synchronized main's stale submodule remote.
- [ ] Preserve main's clean vendor checkout commit `53f9eefffd3278fd430076531548f7b1f5861f9a`
  (reachable from its origin/main), all untracked logs and private previews.
  Reconcile the clean vendor checkout to the already-approved pinned revision;
  never reset or discard file edits.
- [ ] Fast-forward main to the reviewed candidate; run `make test`, `make package`,
  `make smoke` on the candidate with `/Users/taejunoh/.cargo/env` sourced.
- [ ] Record candidate G and push main without force after local gates pass.

## Task 3: Protected validation, tag and public artifact

- [ ] `gh workflow run release.yml --ref main`; verify returned run headSha is G.
- [ ] Stop for the required human environment review if requested; do not modify
  reviewers, deployment policies, credentials or bypass the protection.
- [ ] After successful tagless validation, confirm clean candidate and remote
  tag absence; `git tag -a v0.3.1 G -m 'Needlbar v0.3.1'`, push only that tag.
- [ ] Require successful tag-run publication from its own validated artifact.
- [ ] Download ZIP and SHA256 sidecar afresh from public v0.3.1, check checksum,
  arm64 host/widget, both versions/builds, fixture exclusion, strict signature,
  expected signer/runtime, staple, Gatekeeper and packaged smoke.

## Task 4: Reinstall and distribution documentation

- [ ] Only after public verification update taejunoh/homebrew-tap Casks/needlbar.rb
  to 0.3.1 and the measured public ZIP checksum. Preserve arm64/macOS14/no-zap.
- [ ] Verify the Cask with supported checks. Resolve the running canonical app
  path/PIDs again; keep an exact recoverable backup and stop only that app's
  matching host/widget processes. Preserve other development apps and settings.
- [ ] Install the verified app using the established canonical runtime location
  or the existing Homebrew installation, avoiding a duplicate running copy.
- [ ] Confirm installed executable hashes/signature/version and launch once.
- [ ] Update README public download wording and STATUS with actual release/run/
  checksum/install results and remaining QA gaps; no premature success claims.

## Current checkpoint

Preflight and vendor reachability verification complete. Main is fast-forwarded
to implementation commit `3199665`. Release metadata and prepared/public-state
contracts are complete and independently reviewed. Fresh `make test` passed
(Swift 464 tests / 19 suites), followed by `make package` and `make smoke`.
Final contract refinements were separately reverified with the notarization
shell suite. Candidate commit, main push and protected tagless validation are
next; no release publication or reinstall has occurred.
