# Needlbar v0.3.3 release

User authorized merging PR #4 and publishing a release on 2026-09-16,
including the previously unpushed Claude feasibility work.

Follow the established two-run release procedure: tagless validation of exact
candidate G, then annotated v0.3.3 at the same G and independently validated
publication. Never bypass protected-environment review or rewrite an existing tag.
Use host/widget version 0.3.3 build 6; preserve signing, entitlements and IDs.
Do not reinstall or access live credentials. Preserve dirty main docs and logs.

- [x] Wait for PR #4 CI success and merge matching reviewed head.
- [x] Prepare and verify metadata, release notes and shell contracts.
- [x] Run full make test; candidate metadata is ready for main publication.
- [x] Dispatch tagless Release workflow on exact candidate; await approval.
- [x] After validation succeeds, tag same candidate and await publication.
- [x] Verify public archive checksum, version, signature, notarization.
- [x] Update Homebrew distribution and README only after public verification.

The change provides passive quota fallback, not automatic authentication renewal.
API-balance feasibility tooling is not production balance retrieval. The previous
local passive-fallback make test passed with 497 Swift tests and other gates;
the final release candidate requires its own validation.

Candidate `2d9b79623f82c990e5e17ed676a307ffc9546a3a` passed tagless run
`35084971102` and publication run `35086637114`. The public ZIP checksum is
`79ac52b74972e51eac9c603f62cc6dc8b695a11b333ea92ecbe43e17a7ff7f0f`.
Public version/build, platform, signatures, staple and Gatekeeper checks passed.
No local app installation was performed.
Homebrew tap commit `60aaa30` was pushed. Ruby syntax and brew style passed;
online audit is blocked by local CLT/macOS 27 compatibility before cask analysis.
