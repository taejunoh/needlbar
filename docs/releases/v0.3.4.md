- Claude quota status and its typed safe failure reason are now consistent across the dashboard, provider popover, and Settings. The UI retains the last-known quota and shows its actual last successful check time.
- Added Claude-only failure-origin and attempt-time diagnostics to the existing quota refresh. Events use allowlisted values and do not include credentials, response bodies, account details, or arbitrary error text.
- Fixed diagnostics decoding for the Rust OAuth source spelling `oAuth`; the Swift reader accepts both `oAuth` and `oauth`. The diagnostics envelope carries status and failure metadata, not numeric quota values.
- Local installed-app acceptance on September 22, 2026 showed Claude at 0% and Fable weekly at 38% remaining, with its reset time displayed.
- This confirms that observed refresh and UI state only; it does not guarantee future refresh availability.
- No additional quota request or automatic authentication renewal was added.
